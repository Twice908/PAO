{
  "name": "pao",
  "private": true,
  "description": "Pulse Agent Observe (PAO) — standalone AI agent observability stack extracted from Pulse",
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,worker,web -c blue,green,cyan \"npm run dev -w apps/api\" \"npm run dev -w apps/worker\" \"npm run dev -w apps/web\"",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "tsx scripts/test-pao-e2e.ts"
  },
  "devDependencies": {
    "concurrently": "^10.0.1",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "pnpm": "^11.8.0"
  }
}
